import wollok.game.*
import jugador.*
import configuraciones.*


program game {
	game.title("CORRE WACHIN!")
	game.height(altura)
	game.width(ancho)
	game.cellSize(60)
	game.boardGround("fondoClave.png")
	
	prueba.iniciar()
	
	game.showAttributes(jugador)
	
	game.start()
}
