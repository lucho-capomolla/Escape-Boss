import wollok.game.*
import jugador.*
import configuraciones.*
import menuInicio.*

program juego {
	game.title("Escape Boss")
	game.height(altura)
	game.width(ancho)
	game.cellSize(60)
	configuraciones.configurarTeclas()
	menuInicio.iniciar()
	
	game.boardGround("Fondos/Fondo.png")
	
	game.start()
}
